import { BrowserRouter, Switch, Route } from 'react-router-dom';
import React from 'react';

const Routes = () => (
  <BrowserRouter>
    <Switch>
      <Route />
    </Switch>
  </BrowserRouter>
);

export default Routes;
